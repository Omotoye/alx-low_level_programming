#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers
 * @num1: (int) integer 1; to be added to integer 2
 * @num2: (int) integer 2; to be added to integer 1
 * @brief A function that takes two integers, add
 *     them together and return the sum
 * Return: (int) the sum of the two integer arguments
 * @file 10-add.c
 * @author Omotoye Shamsudeen Adekoya
 */
int add(int num1, int num2)
{
	return (num1 + num2);
}
